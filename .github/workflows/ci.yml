name: example ci

on:
  workflow_call:
    inputs:
      token:
        description: |
          token used for RO privlages
        required: true

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          
